{
  "_baseUrl": "https://mozilla.github.io/nunjucks/templating.html",
  "filters": {
    "default": "#default-value-default-boolean",
    "sort": "#sort-arr-reverse-casesens-attr",
    "striptags": "#striptags-value-preserve_linebreaks",
    "dump": "#dump-object",
    "abs": "#abs",
    "batch": "#batch",
    "capitalize": "#capitalize",
    "center": "#center",
    "dictsort": "#dictsort",
    "escape": "#escape-aliased-as-e",
    "float": "#float",
    "first": "#first",
    "groupby": "#groupby",
    "indent": "#indent",
    "int": "#int",
    "join": "#join",
    "last": "#last",
    "length": "#length",
    "list": "#list",
    "lower": "#lower",
    "random": "#random",
    "rejectattr": "#rejectattr-only-the-single-argument-form",
    "replace": "#replace",
    "reverse": "#reverse",
    "round": "#round",
    "safe": "#safe",
    "selectattr": "#selectattr-only-the-single-argument-form",
    "slice": "#slice",
    "string": "#string",
    "sum": "#sum",
    "title": "#title",
    "trim": "#trim",
    "truncate": "#truncate",
    "upper": "#upper",
    "urlencode": "#urlencode",
    "urlize": "#urlize",
    "wordcount": "#wordcount"
  },
  "tags": {
    "if": "#if",
    "for": "#for",
    "asyncEach": "#asynceach",
    "asyncAll": "#asyncall",
    "macro": "#macro",
    "set": "#set",
    "extends": "#extends",
    "block": "#block",
    "include": "#include",
    "import": "#import",
    "raw": "#raw",
    "filter": "#filter",
    "call": "#call"
  }
}